name: Hello World Action

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: 23f2002020@ds.study.iitm.ac.in
        run: echo "Hello from 23f2002020@ds.study.iitm.ac.in"

      - name: prime-cache-2a6947b
        id: cache
        uses: actions/cache@v4
        with:
          path: ~/.cache
          key: cache-2a6947b

      - name: Echo cache result
        run: echo "Cache hit - ${{ steps.cache.outputs.cache-hit }}"